doctype
html
    head 
        meta(charset = 'utf-8')
        title GIF 채팅방
    body 
        div f12를 눌러서 console 탭과 네트워크 탭을 확인 하세요 
        script.
            var webSocket = new WebSocket("ws://localhost:8005");
            webSocket.onopen = function(){
                console.log('연결 성공');
            }
            webSocket.onmessage = function (event) {
                console.log(event.data);
                webSocket.send('클라이언트에서 답장을 보냄');
            }